Modular workflow blueprint: PDF merger


Objective: Write a Python script that Merges multiple PDF files via terminal by giving the first input as a destination and the inputs after wards as sources to select from to merge and then finally put it into the destination file.


Deduction: 
Here we are going to work back wards from the solution to the problem to understand what we need in big lines before we decomposition the the big lines into smaller workable block loads.

The end product is a script that allows us to merge pdf files from different sources into one centralized pdf file. unifying all the pdf files.

1
so first need to choose which modules we are working with and because we are using the CLI to execute the script we need the cli module here for which is the sys module. and of course as we are working with pdf files we need the pypdf module for this.

2
now that we have all the modules, we would need to that have input destination and sourse

3
then the code must use the sources after the first input in the terminal after the script code and that would be [2] in code as index. and use those sources to combine it into an unified file in the destination.

the new unified file is a object
the obbject that has bbeen merged by pypdf - method?
make a new obbject using pypdf for the destination file
make a new file
asigned the file for that obbject
then asiggn the the sources which has been put in object to the new file.

 
Resources:
-sys module
-pypdf module

Decomposition:

INPUT:
1- take the raw datas
2- put the raw datas in an objects - variables
3- check if data is valid that is put in the object with try and exception.

PROCESSING:
4- initiate the object-sourcedata with the class module trough the use of a method, instantiate a class object from the pypdf module.
5- merge the object-data trough the use of a module method.

OUTPUT:
6- use the designated module method to make the merged object data into a permanent object state.

In code:
script

def main():
	"script function name"()

if __name__ == "__main__":
	main()


Modular Blueprint:

###
1. take the raw datas
Input:
what do i need to do to get the output? and what do i need?
cli mod = import sys
pdf mod = import pypdf

(terminal > input method  = sys.argv[2:] (from after the script input everthing else))

(script file name = which would be name of the script so we can ignore this for  now)
destination_file_name = sys.argv[1]
sources_in_order = sys.argv[2:] 

Output:
in cli command line arguments- script, destination name, source, sources, etc,
###
->
###
2. put the raw datas in an objects - variables

already done in step one
###
->
###
3. check if data is valid that is put in the object with try and exception.
Input:
before resource acquisition:
something with try:
and something with exception:
need to search about both in what way to use it and in which situations or funnel all error into one and make the user exit the request phase 

search how to make the user exit the request phase in cli

After Resource Acquisition:
try:
	destination_file_name = sys.argv[1]
	sources_in_order = sys.argv[2:]
except Exception as e:
	parser.error(e)
	sys.exit()
	
or
except Exception:
	print("Something went wrong, please enter the correct files. Watch out for spelling mistakes!")
	sys.exit()

Output:
data gets error handling based and either exits the input request phase or keeps iterating for a new answer. in this situation it exits the request phase in which you would have to enter the script name and arguments again in order to execute the program/script. 
###
->
###
4. initiate the object-sourcedata with the class module trough the use of a method, instantiate a class object from the pypdf module.

Output:
before resource acquisition:

1. The source_in_order object should be instantiated with the class module pypdf, crafting a new object - new_file - in which you can work with the class properties trough abstraction which is the pypdf module, in this situation to "write" the file method?

Input:
After Resource Acquisition:

from pypdf import PdfMerger

Instantiated environment(module) object -> merger = PdfMerger()

apparently the source object should not be added or done something to the env object. so first craft env object and instantiate that with the aforementioned method

and then in the transformation phase you could relate the source object to the env object.

###
->
###
5. merge the object-data trough the use of a module method.

Output:
before resource acquisition:

1. The instantiated new_file, is now "activated" for transformation. in this situation i need to append the existing sources withing the object, as the object already has multiple pdf files, but has not been appended to each other yet. method? how to append multiple pdf files within the object? every object is a container/array which is something that hold things togheter, within there can be something or nothing depending on what has been added to it. like a list. but every element in the list is its own variable object. so an container of objects if you will. Again what method to use to append pdf files in the object?

Input:
After Resource Acquisition:

for pdf in source_in_order:
	merger.append(pdf)

###
->
###
6. use the designated module method to make the merged object data into a permanent object state.

Output:
before resource acquisition:

1. After transformation, we need to make the modification permanent/"write" into the memory. what method to use to save the new object state?

Input:
After Resource Acquisition:

merger.write("Merged.pdf")

###

Resources:
-google.com

-youtube.com

-https://stackoverflow.com/questions/3444645/merge-pdf-files

-https://pypdf.readthedocs.io/en/stable/user/merging-pdfs.html

Only for general feedback and small offloading tasks:
-Gemini AI

Conclusion:

Blue print completed. Ready to write the code based on the Workflow chart.
Clunky at the beginning. had to like understand what i was searching for. formulating the search key words is an important step to implement in order to get proper results. finally learned the most important thing is to go trough documents first when it comes to every module. as the documents has lot of information that most of the type should be available and sometimes the documents even uses references such us stack overflow for user based situations where you can look how others write their code and reference their code in this manner.

Again clunky, but as i do this more i will become better at engineering code by repeatedly implementing objective decomposition method, modular blueprint, and assigning contracts in sha Allah.
Blueprint, Decompose, Acquire, Implement
